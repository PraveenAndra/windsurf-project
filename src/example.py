def greet(name: str) -> str:
    """Return a greeting message for the given name."""
    return f"Hello, {name}! Welcome to the Python project template."


def add(a: int, b: int) -> int:
    """Add two numbers together."""
    return a + b
